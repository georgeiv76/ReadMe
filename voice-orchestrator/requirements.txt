# Core pipeline runs with the standard library alone (stub backend), so you
# can test the orchestrator logic anywhere with zero installs.

# --- Recording (your local machine, needs a microphone) ---
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24

# --- Config (optional; a built-in mini-parser is used if absent) ---
PyYAML>=6.0

# --- Claude Desktop integration (local MCP server) ---
mcp[cli]>=1.2.0

# --- Voice-clone backend (default: Coqui XTTS) ---
# These pins were validated together; run `bash setup.sh` which installs them.
# Loosening them breaks: transformers 5.x removes a symbol coqui imports, and
# torch>=2.9 requires torchcodec.
coqui-tts==0.27.5
transformers==4.57.1
torch>=2.1
torchaudio>=2.1
torchcodec>=0.1
# First synthesis downloads XTTS-v2 (~1.8 GB) and needs COQUI_TOS_AGREED=1.
#
# Alternative backend — OpenVoice (best intonation/style control), install from
# source per https://github.com/myshell-ai/OpenVoice, then set engine: openvoice

# --- Quality evaluation (speaker-similarity gate in the refine loop) ---
resemblyzer>=0.1.4
